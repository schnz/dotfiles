#!/bin/bash

gdbus call --session --dest org.gnome.Shell.Screenshot --object-path /org/gnome/Shell/Screenshot --method org.gnome.Shell.Screenshot.PickColor | grep -Eoz "[0-9][^)]+" | xargs -0 printf -- "[%s]" | jq 'map(.*255)' | grep -Eo '[0-9]+' | xargs printf -- "#%x%x%x\n"
